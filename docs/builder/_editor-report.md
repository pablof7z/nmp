# Editor report — NMP Builder Manual coherence pass

Scope: read all 33 chapters + `000`/`001` front matter, wrote the landing index, reconciled status/links/navigation, and flagged residual contradictions. No chapter substance was rewritten; the writers' code-grounded examples and honest gap-flags are intact.

## What I wrote
- **`README.md`** — the site landing page: title, 2-sentence intro (embeddable engine, library-not-framework, two nouns, values-in-code-after), a **Start here** block (→ *10-minute timeline* + *mental model in one diagram*), and the full 7-Part TOC as a linked list with per-chapter BUILT/PARTIAL/PLANNED status and ★ marks.
- **Nav footers** on all 33 chapters — `← prev · Index · next →` in TOC order (idempotent, marked with an HTML comment so a re-run won't duplicate them).

## Inconsistencies fixed
1. **Diagnostics status drift (the main one).** Chapter 03 called the diagnostics surface **PARTIAL / "in progress" / "not yet built"** in three places (the chapter status-map row for ch 22, the milestone-narrative "Not yet built" sentence, and a trailing "honest note"). This contradicted the canonical TOC in `001`, chapter 22's own **BUILT ★** banner, and the task's ground truth. Fixed all three to **BUILT** (engine + FFI landed and live-proven; Falsifier diagnostics screen ships today).
2. **Mislabeled cross-link.** Chapter 02 linked `*[How the engine works](11-coverage.md)*` — a title with no matching chapter in the final TOC. Relabeled to the real target title, *Coverage: empty vs unknown*.
3. **Milestone-table sub-claim.** Chapter 03's M5 row said "planning (diagnostic surface + the app)"; the diagnostic-surface half is built. Narrowed to "planning (the app itself; its diagnostic surface has landed)."
4. **Status-banner + TOC audit.** Verified every chapter's top banner against the canonical status list in `001`. **All 33 now agree** (01–11 BUILT, 12–13 PLANNED, 14 BUILT/PARTIAL-aside, 15 PARTIAL, 16–19 BUILT, 20 PARTIAL, 21 BUILT, 22–26 BUILT, 27 PARTIAL, 28–31 BUILT, 32 PLANNED, 33 BUILT). No further banner drift.
5. **Link integrity.** Every `NN-slug.md` cross-link in the corpus resolves to a real file (checked exhaustively). No dangling "another agent owns ch. X" placeholders exist.

## Residual items a human should know
- **Falsifier "built vs. planning" tension (real, unresolved — flag, not fixed).** Chapters 04, 22, 25, and 31 treat the Falsifier app as **running code today** (`apps/Falsifier/...`, "ships today," real tests). Chapter 03's milestone table still lists **M5 (iOS falsifier app) as "planning"** and ch 03 line 95 tags ch 31 as "(falsifier itself M5)". I reconciled only the *diagnostic surface* (which the task designated BUILT) and left the app-level milestone claim intact to avoid rewriting substance. A human should decide the single truth: is the Falsifier a shipped app or an in-progress M5 deliverable? Whichever it is, ch 03's milestone row and the ch 04/22/25/31 "ships today" framing should be made to say the same thing.
- **Cross-reference style is mixed (intentional, left as-is).** Roughly half the chapters (notably 22–33, 27) use full markdown links for cross-refs; the other half (e.g. 04, 05, 09, 10, 14–21) use plain italic chapter titles without links — which the writing brief (§7) explicitly sanctions ("cross-reference by chapter title in prose"). The new nav footers guarantee navigability regardless. Converting the italic refs to links everywhere would be a large, low-risk-but-out-of-scope edit; noting it rather than doing it.
- **Minor title-wording variance in prose refs.** A few italic references say "Live queries *and* the binding grammar" where the chapter title uses "&". Cosmetic; not touched.
- **Duplicated worked example (by design, worth knowing).** The `$myFollows` / `FeedFilters.follows` snippet and the `DiagnosticsView` snippet recur across chapters 04, 05, 06, 09, 10, 22, and 31. This is deliberate pedagogical repetition (each chapter is self-contained per the brief), not contradiction — the code agrees everywhere. No action needed unless a future edit changes the API in one place; if so, all copies must move together.
- **Ch 30 H1 lists platforms as "iOS, Rust, Android, TS, TUI"** while the canonical TOC says "iOS, Android, Rust, TS, TUI." Ordering only; substance identical. Left as the author wrote it.
