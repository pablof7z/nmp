[package]
name = "nmp-nip29"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# #1551: NIP-51 defines kind:10009; `nmp-nip29` exposes NMP's observational
# value and query beside the group vocabulary that consumes it. It remains a
# pure `nostr` + `nmp-grammar` package: no router, resolver, engine, signer, or
# write dependency.
[dependencies]
nostr.workspace = true
nmp-grammar.workspace = true
