[package]
name = "nmp-surface-component-catalog"
version = "0.0.0"
edition = "2021"
publish = false

# Keep this governance tool outside NMP's product/workspace dependency graph.
[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
