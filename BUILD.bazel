# @generated by tools/bazel/gen_buildfiles.py -- do not edit by hand.
# Root package. Exports the crate_universe source of truth and the
# workspace fixture, and aggregates every first-party crate's source
# tree into //:first_party_sources for source-scanning tests.
exports_files(["Cargo.toml", "Cargo.lock"])

# Workspace-root fixture included via include_str! from nmp-test-support
# (crates/nmp-test-support/src/reference_fixtures.rs -> ../../../fixtures/).
exports_files(["fixtures/reference-locators.json"])

filegroup(
    name = "first_party_sources",
    srcs = ["//crates/nmp-bookmarks:crate_files", "//crates/nmp-consumer-check:crate_files", "//crates/nmp-content:crate_files", "//crates/nmp-engine:crate_files", "//crates/nmp-grammar:crate_files", "//crates/nmp-local-signer:crate_files", "//crates/nmp-nip02:crate_files", "//crates/nmp-nip11:crate_files", "//crates/nmp-nip18:crate_files", "//crates/nmp-nip22:crate_files", "//crates/nmp-nip25:crate_files", "//crates/nmp-nip29:crate_files", "//crates/nmp-nip65-consumer-check:crate_files", "//crates/nmp-nip65:crate_files", "//crates/nmp-nip73:crate_files", "//crates/nmp-nipc7:crate_files", "//crates/nmp-outbox:crate_files", "//crates/nmp-resolver-testkit:crate_files", "//crates/nmp-resolver:crate_files", "//crates/nmp-router-testkit:crate_files", "//crates/nmp-router:crate_files", "//crates/nmp-runtime:crate_files", "//crates/nmp-signer:crate_files", "//crates/nmp-store:crate_files", "//crates/nmp-test-support:crate_files", "//crates/nmp-transport:crate_files", "//crates/nmp:crate_files"],
    visibility = ["//visibility:public"],
)

# Root non-crate inputs staged as runfiles for targets that read
# workspace-level data via env!("CARGO_MANIFEST_DIR")-relative paths.
filegroup(
    name = "workspace_data",
    srcs = [
        "Cargo.toml",
        "Cargo.lock",
    ],
    visibility = ["//visibility:public"],
)

# `bazel run //:fmt` rewrites every crate's sources in place, the way
# `cargo fmt` does. The read-only counterpart is `--config=fmt-check`,
# which reports without writing (see .bazelrc).
alias(
    name = "fmt",
    actual = "@rules_rust//tools/rustfmt",
    visibility = ["//visibility:public"],
)
