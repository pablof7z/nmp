[package]
name = "nmp-nip02"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# #1143: pure vocabulary only -- `nostr` + `nmp-grammar`, no engine, no
# mechanism. `nmp` must never appear here again; that reverse edge is the
# exact defect #1143 closed.
[dependencies]
nostr.workspace = true
nmp-grammar.workspace = true
