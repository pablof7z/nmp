//! Scaffold stub. The fake-relay / ingest harness + event builders (§2.3 of
//! `docs/plans/M1-grammar-engine-plan.md`) are built by the resolver builder
//! (Steps 3-7). This module exists only so `nmp-resolver::testkit` resolves
//! for the workspace build.
