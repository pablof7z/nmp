[toolchain]
# Pinned nightly used for the reviewable rust-toolchain-versions.env oracle and
# any workspace builds that rely on that exact toolchain. Keep in sync with
# tools/rust-toolchain-versions.env.
channel = "nightly-2026-07-07"
components = ["rustfmt", "clippy"]
