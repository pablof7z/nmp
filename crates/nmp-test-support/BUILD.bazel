# @generated by tools/bazel/gen_buildfiles.py -- do not edit by hand.
# First-party BUILD for the `nmp-test-support` crate. Third-party deps come from
# @crates via all_crate_deps(); first-party path deps + the Cargo feature
# model (lib vs lib_test) are explicit. Regenerate with:
#   python3 tools/bazel/gen_buildfiles.py

load("@crates//:defs.bzl", "aliases", "all_crate_deps", "crate_edition")
load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_library", "rust_test")

rust_library(
    name = "lib",
    crate_name = "nmp_test_support",
    srcs = glob(["src/**/*.rs"]),
    edition = crate_edition(),
    aliases = aliases(),
    deps = all_crate_deps(normal=True),
    proc_macro_deps = all_crate_deps(proc_macro=True),
    compile_data = ["Cargo.toml", "//:fixtures/reference-locators.json"],
    visibility = ["//visibility:public"],
)

rust_library(
    name = "lib_test",
    crate_name = "nmp_test_support",
    srcs = glob(["src/**/*.rs"]),
    edition = crate_edition(),
    aliases = aliases(),
    deps = all_crate_deps(normal=True),
    proc_macro_deps = all_crate_deps(proc_macro=True),
    compile_data = ["Cargo.toml", "//:fixtures/reference-locators.json"],
    visibility = ["//visibility:public"],
)

rust_test(
    name = "unit_tests",
    crate = ":lib_test",
    edition = crate_edition(),
    aliases = aliases(normal_dev=True, proc_macro_dev=True),
    deps = all_crate_deps(normal=True, normal_dev=True),
    proc_macro_deps = all_crate_deps(proc_macro=True, proc_macro_dev=True),
    rustc_env = {"CARGO_MANIFEST_DIR": "./crates/nmp-test-support"},
    data = ["//:first_party_sources", "//:workspace_data"],
    visibility = ["//visibility:public"],
)

rust_binary(
    name = "android_controlled_relay",
    srcs = ["src/bin/android_controlled_relay.rs"],
    edition = crate_edition(),
    aliases = aliases(),
    deps = all_crate_deps(normal=True) + [":lib"],
    proc_macro_deps = all_crate_deps(proc_macro=True),
    visibility = ["//visibility:public"],
)

rust_binary(
    name = "outbox-routing-relay-harness",
    srcs = ["src/bin/outbox-routing-relay-harness.rs"],
    edition = crate_edition(),
    aliases = aliases(),
    deps = all_crate_deps(normal=True) + [":lib"],
    proc_macro_deps = all_crate_deps(proc_macro=True),
    visibility = ["//visibility:public"],
)

filegroup(
    name = "crate_files",
    srcs = glob(["**"], exclude = ["target/**", "target"]),
    visibility = ["//visibility:public"],
)
